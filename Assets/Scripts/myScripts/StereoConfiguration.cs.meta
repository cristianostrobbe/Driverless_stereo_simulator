using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StereoConfiguration : MonoBehaviour
{

    
    public Camera cameraLeft;
    public Camera cameraRight;

    // Camera.enabled = true // Enable or disable the camera

    // Start is called before the first frame update
    void Start()
    {
        // Camera parameters initialisation
        //cameraLeft = GameObject.FindGameObjectWithTag("leftCamera").GetComponent<Camera>();
        //Debug.Log(cameraLeft.transform.position);

    }

    // Update is called once per frame
    void Update()
    {
        Debug.Log(cameraLeft.transform.position);
    }
}
